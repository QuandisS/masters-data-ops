services:
  api:
    build: .
    container_name: diabetes_api
    ports:
      - "8000:8000"
    environment:
      - MLFLOW_TRACKING_URI=http://localhost:5000
    networks:
      - ml_network

networks:
  ml_network:
    driver: bridge